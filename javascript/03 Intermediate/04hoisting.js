tipper("80");

function tipper(a) {
  var bill = parseInt(a); //parseInt:-if the number is rapped around string then it gone check the string and make it no if it's string than gone give NaN error (number not found)
  console.log(bill + 5);
}

var bigTipper = function (a) {
  var bill = parseInt(a);
  console.log(bill + 15);
};

bigTipper("200");

console.log(name);
var name = "hitesh";

function sayName() {
  var name = "MR. H";
  console.log(name);
}
sayName();

console.log(name);

